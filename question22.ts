// Question No 22 //


// Intentional Error: If you haven’t received an array index error in one of your programs yet, try to make one happen. Change an index in one of your programs to produce an index error. Make sure you correct the error before closing the program.

// Making Array .//

let numbers = [0,1,2]; 
let index = 3 // Make A Array Of Index That Can Give Us Error
 if(index < numbers.length){ // Using if else printing Index < numbers.length
    console.log(numbers[index])
 }else{
    console.log("Error Index Out Of Found") // if our index number is 3 then this code print this otherwise print the number//
 };